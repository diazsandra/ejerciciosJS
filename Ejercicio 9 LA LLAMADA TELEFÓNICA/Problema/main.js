var T = prompt("Indica los minutos que duró la llamada")
var C = prompt("Indica el costo por minuto")
var L = (parseInt(T)*parseInt(C))

alert("El costo de una llamada de "+T+" minutos es de $"+L+" pesos.")